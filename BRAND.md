# VKEN Design Engine

VKEN Design Engine is the user-facing brand for this repository. Internally, the workspace keeps the existing `@open-design/*` package names because they are part of the daemon, app, and adapter contract; broad package-scope renames are deferred until they can be done without disrupting the build.
